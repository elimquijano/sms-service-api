{
  "name": "sms-service-api",
  "version": "1.0.0",
  "description": "API durable y segura para encolar y despachar SMS hacia un gateway Android",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --test-concurrency=1",
    "check": "node --check server.js && node --check src/store.js && node --check src/dispatcher.js && node --check src/sms.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "winston": "^3.17.0",
    "winston-daily-rotate-file": "^5.0.0",
    "ws": "^8.18.2"
  }
}
